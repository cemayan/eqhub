

spring:
  webflux:
    base-path: '/api'
  data:
    mongodb:
      host: localhost
      port: 27017
      database: eqhub
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
          jwk-set-uri: http://localhost:9000/oauth2/jwks
server:
  port: 8085